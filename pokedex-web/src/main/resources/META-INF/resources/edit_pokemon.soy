{namespace pokedex_edit_pokemon}

/**
 * Prints the Pokedex portlet page to add/edit a Pokemon.
 *
 * @param backPortletURL
 */
{template .render}
	Add or Edit a pokemon here. <a href="{$backPortletURL}">Back to the view page.</a>
{/template}